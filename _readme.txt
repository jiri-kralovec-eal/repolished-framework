If you haven't used NPM before, you need to install Node.JS (https://nodejs.org/en/)

The approach is pretty straight forward, run cmd in the folder followed by :

npm init
npm i

The workflow is started by typing 'gulp', 'gulp --type=prod' will generate production files